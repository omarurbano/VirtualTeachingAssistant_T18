# ─── Core ───────────────────────────────────────────────────────────────────────
numpy>=1.24
requests>=2.31
python-dotenv>=1.0

# ─── Document Text Extraction ───────────────────────────────────────────────────
# On Linux/Brev: also install system package `libmagic1`
#   sudo apt-get update && sudo apt-get install -y libmagic1
unstructured[all-docs]==0.18.32

# ─── SharePoint / Microsoft Graph API ──────────────────────────────────────────
msal>=1.25            # Microsoft Authentication Library

# ─── Data & Model Handling ─────────────────────────────────────────────────────
datasets>=2.14          # HuggingFace Datasets
transformers>=4.35      # HuggingFace Transformers
peft>=0.7               # LoRA / QLoRA adapter library
bitsandbytes>=0.41      # QLoRA 4-bit quantization
trl>=0.8                # SFTTrainer for supervised fine-tuning
accelerate>=0.25        # distributed training helper
safetensors>=0.4        # safe model serialization

# ─── Utilities ─────────────────────────────────────────────────────────────────
tqdm>=4.66
pyyaml>=6.0
