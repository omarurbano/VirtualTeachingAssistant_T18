# ─── Core ───────────────────────────────────────────────────────────────────────
numpy>=1.24
requests>=2.31
python-dotenv>=1.0

# ─── Google Drive API ──────────────────────────────────────────────────────────
google-api-python-client>=2.100
google-auth-httplib2>=0.1
google-auth-oauthlib>=1.1

# ─── Document Text Extraction ──────────────────────────────────────────────────
unstructured[all-docs]>=0.12  # includes pdf, docx, pptx, html parsers

# ─── Optional: faster PDF ──────────────────────────────────────────────────────
# pdfplumber>=0.10  # uncomment if you need table extraction from PDFs
# pytesseract>=0.3   # uncomment for OCR on scanned PDFs

# ─── Data & Model Handling ─────────────────────────────────────────────────────
datasets>=2.14          # HuggingFace Datasets (for LoRA fine-tuning prep)
transformers>=4.35      # HuggingFace Transformers
peft>=0.7               # LoRA / QLoRA adapter library
bitsandbytes>=0.41      # QLoRA 4-bit quantization
trl>=0.8                # SFTTrainer for supervised fine-tuning
accelerate>=0.25        # distributed training helper
safetensors>=0.4        # safe model serialization

# ─── Utilities ─────────────────────────────────────────────────────────────────
tqdm>=4.66
pyyaml>=6.0
